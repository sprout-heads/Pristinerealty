@page
@model Pristinerealty.Web.Pages.BlogModel
@{ ViewData["Title"] = "Blog page"; }
<!DOCTYPE html>
<html lang="en">
<head>
 
    @*<meta name="viewport" content="width=device-width, initial-scale=1.0" />*@
    <title>@ViewData["Title"] - </title>

 
    <title>Blog</title>
    <style>
        p {
            word-break: break-all;
        }

        .show-read-more .more-text {
            display: none;
        }
    </style>


    <script>
        $(document).ready(function () {
            var maxLength = 300;
            $(".show-read-more").each(function () {
                var myStr = $(this).text();
                if ($.trim(myStr).length > maxLength) {
                    var newStr = myStr.substring(0, maxLength);
                    var removedStr = myStr.substring(maxLength, $.trim(myStr).length);
                    $(this).empty().html(newStr);
                    $(this).append(' <a href="javascript:void(0);" class="read-more">read more...</a>');
                    $(this).append('<span class="more-text">' + removedStr + '</span>');
                }
            });
            $(".read-more").click(function () {
                $(this).siblings(".more-text").contents().unwrap();
                $(this).remove();
            });
        });
    </script>

</head>
<body>
    <header>
        <!-- header top area start -->
        <!-- header top area end -->
        <!-- ....................service -->

        <section class="rocket   pt-5">
            <div class="container">
                <div class="row">
                    <div class="col-lg-12 ">
                        <h1>Blog</h1>
                        <span class="under">
                            <hr>
                        </span>
                        <p>Located in the heart of Calicut city, and one of the oldest hospitals in the region, the hospital was established in 1940 by Late Dr. P. Balram. From it's early years, the hospital and it's doctors focused on providing access to medical care to the common man, placing service to the poor of utmost importance. His sons, Late Dr. P. Rajendran, Late Dr. P Mohanraj and Late Dr. P. Ramesh Kumar continued the tradition. The hospital is an integral part of the history of Calicut.</p>
                    </div>
                </div>
            </div>
            </div>
        </section>

        @foreach (var item in Model.article)
        {

            <!-- ...............................blog section........................... -->
            <section class="why-choose section">
                <div class="container">
                    <div class="row">
                        <div class="col-lg-12">
                            <div class="section-title">
                                <h2>@item.Title</h2>
                                <center>     <hr style="background-color: #000;width: 120px;"></center>

                            </div> 
                        </div>
                    </div>
                    <div class="row">
                        <div class="col-lg-6 col-12">
                            <div class="choose-left">

                                <div class="show-read-more">
                                    @Html.Raw(item.Content)
                                    @*<button class="btn-nm" onclick="myFunction()" id="myBtn">Read more</button>*@
                                </div>
                            </div>
                        </div>
                        <div class="col-lg-6 col-12">
                            <img src="@item.ImagePath" asp-append-version="true" class="img-responsive">
                        </div>
                    </div>
                </div>
            </section>

        }






        <section>
            <div class="container pb-5">
                <div class="row">
                    <div class="col-lg-12">
                        <di>

                        </di>
                        <p class="text-center">
                        </p>

                    </div>

                </div>

            </div>
        </section>

    </header>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/slick-carousel/1.6.0/slick.js"></script>
    <script src="~/js/jquery.min.js"></script>
    <script src="~/js/popper.min.js"></script>
    <script src="~/js/bootstrap.min.js"></script>

    @*<script>
        window.onscroll = function () { myFunction() };
        var header = document.getElementById("header_bottom");
        var sticky = header.offsetTop;
        function myFunction() {
            if (window.pageYOffset > sticky) {
                header.classList.add("sticky");
            } else {
                header.classList.remove("sticky");
            }
        }
    </script>


    <script>
        // optional
        $('#blogCarousel').carousel({
            interval: 5000
        });
    </script>*@



    <!-- ...................service..................section -->


    <script>
        $('.features-item').on('click', function () {
            var $this = $(this);
            var imgsrc = $this.attr('data-src');
            $('.features-image img').attr('src', imgsrc);
        });</script>



    <script>
        $(document).ready(function () {
            var maxLength = 300;
            $(".show-read-more").each(function () {
                var myStr = $(this).text();
                if ($.trim(myStr).length > maxLength) {
                    var newStr = myStr.substring(0, maxLength);
                    var removedStr = myStr.substring(maxLength, $.trim(myStr).length);
                    $(this).empty().html(newStr);
                    $(this).append(' <a href="javascript:void(0);" class="read-more">read more...</a>');
                    $(this).append('<span class="more-text">' + removedStr + '</span>');
                }
            });
            $(".read-more").click(function () {
                $(this).siblings(".more-text").contents().unwrap();
                $(this).remove();
            });
        });
    </script>





</body>
</html>